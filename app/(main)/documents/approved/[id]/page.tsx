'use client';

import React from 'react';

import Review from '@components/TabView/Review';

const Document = () => {
    return (
        <section>
            <Review />
        </section>
    );
};

export default Document;
