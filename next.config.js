/** @type {import('next').NextConfig} */
const nextConfig = {
    /*output:"export",
    basePath:"/cacao",*/
    images: {
        domains: ['dummyimage.com']
    },
    reactStrictMode: true,
};

module.exports = nextConfig;
