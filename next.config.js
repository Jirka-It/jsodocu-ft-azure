/** @type {import('next').NextConfig} */
const nextConfig = {
    /*output:"export",
    basePath:"/cacao",*/
    images: {
        domains: ['dummyimage.com']
    },
    reactStrictMode: false
};

module.exports = nextConfig;
